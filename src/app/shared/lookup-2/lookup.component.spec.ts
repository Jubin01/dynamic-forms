import { TestBed, inject, waitForAsync } from '@angular/core/testing';


describe('LookupComponent2 tests', () => {

    it('checks if onKeyUp() exists', () => {
        expect('hellotest').toBe('hellotest');
    });

    it('checks if lookupClear() exists', () => {
        expect('hellotest').toBe('hellotest');
    });

    it('checks if validate function exists', () => {
        expect('hellotest').toBe('hellotest');
    });

});
